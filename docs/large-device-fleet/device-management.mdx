# Device management

import useBaseUrl from "@docusaurus/useBaseUrl";

Your LoRaWAN network server (Router) has some basic device management capabilities that address device’s LoRaWAN functions. To configure other device parameters and application-specific settings, you will most likely need to use device manufacturer’s tools.
