# Device management

import useBaseUrl from "@docusaurus/useBaseUrl";

Your Router (LoRaWAN Network Server) has some basic device management
capabilities that address device’s LoRaWAN features and settings. To configure
other device parameters and application-specific settings, you will need to use
device manufacturer’s tools.
